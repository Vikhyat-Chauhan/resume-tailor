# resume-tailor
CLI agent that rewrites your resume's summary and skills sections for a specific job description using Claude API. Reads from a master resume JSON and outputs versioned PDFs per application.
